from setuptools import setup, find_packages

setup(
    name='txgrpc',
    version='0.1',
    description='GRPC libary for twisted',
    long_description='',
    author='Jason Michalski',
    author_email='armooo@armooo.net',
    packages=find_packages(),
    install_requires=[
    ],
)
